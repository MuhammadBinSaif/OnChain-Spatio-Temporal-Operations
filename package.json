{
  "name": "geohash-spatio-temporal-smart-contract",
  "private": true,
  "type": "module",
  "scripts": {
    "compile": "hardhat compile",
    "deploy": "hardhat run --network benchnet --no-compile scripts/deploy.js",
    "deploy:local": "hardhat run --network benchnet --no-compile scripts/deploy.js",
    "deploy:sepolia": "hardhat run --network sepolia --no-compile scripts/deploy.js",
    "benchmark:geohash": "hardhat run --network benchnet --no-compile scripts/benchmark-geohash.js",
    "benchmark:geohash:matrix": "hardhat run --network benchnet --no-compile scripts/benchmark-geohash-matrix.js"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-ethers": "^4.0.0",
    "ethers": "^6.15.0",
    "hardhat": "^3.0.9"
  }
}
